export enum EPlayerState {
    Idle = 'Idle',
    IdleLeft = 'IdleLeft',
    Run = 'Run',
    RunLeft = 'RunLeft',
    Jump = 'Jump',
    JumpLeft = 'JumpLeft',
    Fall = 'Fall',
    FallLeft = 'FallLeft',
}
